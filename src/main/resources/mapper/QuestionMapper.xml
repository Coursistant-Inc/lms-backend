<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.coursistant.lms.mapper.quiz.QuestionMapper">

    <sql id="Base_Column_List">
        id, `type`, stem, choices, answer_key, `status`, updated_at
    </sql>

    <select id="selectAll" resultType="com.coursistant.lms.entity.Question">
        select
        <include refid="Base_Column_List"/>
        from Question
        <where>
            <if test="id != null">and id = #{id}</if>
            <if test="type != null">and `type` = #{type}</if>
            <if test="stem != null">and stem = #{stem}</if>
            <if test="choices != null">and choices = #{choices}</if>
            <if test="answerKey != null">and answer_key = #{answerKey}</if>

            <if test="status != null">and `status` = #{status}</if>
            <if test="updatedAt != null">and updated_at = #{updatedAt}</if>
        </where>
    </select>

    <select id="selectById" resultType="com.coursistant.lms.entity.Question">
        select
        <include refid="Base_Column_List"/>
        from Question
        where id = #{id}
    </select>

    <delete id="deleteById">
        delete from Question where id = #{id}
    </delete>

    <insert id="insert" parameterType="com.coursistant.lms.entity.Question" useGeneratedKeys="true" keyProperty="id">
        insert into Question
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">id,</if>
            <if test="type != null">`type`,</if>
            <if test="stem != null">stem,</if>
            <if test="choices != null">choices,</if>
            <if test="answerKey != null">answer_key,</if>

            <if test="status != null">`status`,</if>
            <if test="updatedAt != null">updated_at,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null">#{id},</if>
            <if test="type != null">#{type},</if>
            <if test="stem != null">#{stem},</if>
            <if test="choices != null">#{choices},</if>
            <if test="answerKey != null">#{answerKey},</if>

            <if test="status != null">#{status},</if>
            <if test="updatedAt != null">#{updatedAt},</if>
        </trim>
    </insert>

    <update id="updateById" parameterType="com.coursistant.lms.entity.Question">
        update Question
        <set>
            <if test="type != null">`type` = #{type},</if>
            <if test="stem != null">stem = #{stem},</if>
            <if test="choices != null">choices = #{choices},</if>
            <if test="answerKey != null">answer_key = #{answerKey},</if>
            <if test="status != null">`status` = #{status},</if>
            <if test="updatedAt != null">updated_at = #{updatedAt},</if>
        </set>
        where id = #{id}
    </update>

</mapper>
