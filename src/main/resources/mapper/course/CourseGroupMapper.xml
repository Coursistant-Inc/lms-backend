<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.coursistant.lms.module.course.group.repository.CourseGroupMapper">

    <sql id="Base_Column_List">
        id,
        group_set_id AS groupSetId,
        course_id AS courseId,
        name,
        capacity_override AS capacityOverride,
        created_at AS createdAt,
        updated_at AS updatedAt
    </sql>

    <insert id="insert" parameterType="com.coursistant.lms.module.course.group.entity.CourseGroup"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO course_group (
            group_set_id, course_id, name, capacity_override
        ) VALUES (
            #{groupSetId}, #{courseId}, #{name}, #{capacityOverride}
        )
    </insert>

    <update id="updateById" parameterType="com.coursistant.lms.module.course.group.entity.CourseGroup">
        UPDATE course_group
        <set>
            <if test="name != null">name = #{name},</if>
            capacity_override = #{capacityOverride},
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM course_group WHERE id = #{id}
    </delete>

    <select id="selectById" resultType="com.coursistant.lms.module.course.group.entity.CourseGroup">
        SELECT <include refid="Base_Column_List"/>
        FROM course_group
        WHERE id = #{id}
    </select>

    <select id="selectByIdForUpdate" resultType="com.coursistant.lms.module.course.group.entity.CourseGroup">
        SELECT <include refid="Base_Column_List"/>
        FROM course_group
        WHERE id = #{id}
        FOR UPDATE
    </select>

    <select id="selectByGroupSetId" resultType="com.coursistant.lms.module.course.group.entity.CourseGroup">
        SELECT <include refid="Base_Column_List"/>
        FROM course_group
        WHERE group_set_id = #{groupSetId}
        ORDER BY id ASC
    </select>

    <select id="countByGroupSetId" resultType="int">
        SELECT COUNT(*) FROM course_group WHERE group_set_id = #{groupSetId}
    </select>

</mapper>
