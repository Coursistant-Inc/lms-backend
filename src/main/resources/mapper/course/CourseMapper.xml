<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.coursistant.lms.module.course.repository.CourseMapper">

    <sql id="Base_Column_List">
        id,
        tenant_id AS tenantId,
        course_code AS courseCode,
        title,
        term_start_date AS termStartDate,
        term_end_date AS termEndDate,
        description,
        location,
        instructor_id AS instructorId,
        state,
        archived_at AS archivedAt,
        creator_id AS creatorId,
        created_at AS createdAt,
        updated_at AS updatedAt
    </sql>

    <select id="selectById" resultType="com.coursistant.lms.module.course.entity.Course">
        SELECT
        <include refid="Base_Column_List"/>
        FROM Course
        WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="com.coursistant.lms.module.course.entity.Course"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO Course (
            tenant_id,
            course_code,
            title,
            term_start_date,
            term_end_date,
            description,
            location,
            instructor_id,
            state,
            archived_at,
            creator_id
        ) VALUES (
            #{tenantId},
            #{courseCode},
            #{title},
            #{termStartDate},
            #{termEndDate},
            #{description},
            #{location},
            #{instructorId},
            #{state},
            #{archivedAt},
            #{creatorId}
        )
    </insert>

    <update id="updateById" parameterType="com.coursistant.lms.module.course.entity.Course">
        UPDATE Course
        <set>
            <if test="courseCode != null">course_code = #{courseCode},</if>
            <if test="title != null">title = #{title},</if>
            <if test="termStartDate != null">term_start_date = #{termStartDate},</if>
            <if test="termEndDate != null">term_end_date = #{termEndDate},</if>
            <if test="description != null">description = #{description},</if>
            <if test="location != null">location = #{location},</if>
            <if test="instructorId != null">instructor_id = #{instructorId},</if>
        </set>
        WHERE id = #{id}
    </update>

    <update id="archiveById">
        UPDATE Course
        SET state = 'Archived',
            archived_at = #{archivedAt}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM Course
        WHERE id = #{id}
    </delete>

</mapper>
