<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.coursistant.lms.module.course.enrollment.repository.EnrollmentMapper">

    <sql id="Base_Column_List">
        id,
        course_id AS courseId,
        user_id AS userId,
        course_role AS courseRole,
        can_grade AS canGrade,
        can_post_announcements AS canPostAnnouncements,
        can_manage_groups AS canManageGroups,
        can_manage_course_events AS canManageCourseEvents,
        active,
        assignment_submit_frozen AS assignmentSubmitFrozen,
        enrolled_at AS enrolledAt,
        withdrawn_at AS withdrawnAt,
        withdrawn_by_actor_type AS withdrawnByActorType,
        withdrawn_by_actor_id AS withdrawnByActorId,
        created_at AS createdAt,
        updated_at AS updatedAt
    </sql>

    <insert id="insert" parameterType="com.coursistant.lms.module.course.enrollment.entity.Enrollment"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO enrollment (
            course_id,
            user_id,
            course_role,
            can_grade,
            can_post_announcements,
            can_manage_groups,
            can_manage_course_events,
            active,
            assignment_submit_frozen,
            enrolled_at,
            withdrawn_at,
            withdrawn_by_actor_type,
            withdrawn_by_actor_id
        ) VALUES (
            #{courseId},
            #{userId},
            #{courseRole},
            #{canGrade},
            #{canPostAnnouncements},
            #{canManageGroups},
            #{canManageCourseEvents},
            #{active},
            <choose>
                <when test="assignmentSubmitFrozen != null">#{assignmentSubmitFrozen}</when>
                <otherwise>0</otherwise>
            </choose>,
            #{enrolledAt},
            #{withdrawnAt},
            #{withdrawnByActorType},
            #{withdrawnByActorId}
        )
    </insert>

    <update id="updateById" parameterType="com.coursistant.lms.module.course.enrollment.entity.Enrollment">
        UPDATE enrollment
        <set>
            <if test="courseRole != null">course_role = #{courseRole},</if>
            <if test="canGrade != null">can_grade = #{canGrade},</if>
            <if test="canPostAnnouncements != null">can_post_announcements = #{canPostAnnouncements},</if>
            <if test="canManageGroups != null">can_manage_groups = #{canManageGroups},</if>
            <if test="canManageCourseEvents != null">can_manage_course_events = #{canManageCourseEvents},</if>
            <if test="active != null">active = #{active},</if>
            <if test="assignmentSubmitFrozen != null">assignment_submit_frozen = #{assignmentSubmitFrozen},</if>
            <if test="withdrawnAt != null">withdrawn_at = #{withdrawnAt},</if>
            <if test="withdrawnByActorType != null">withdrawn_by_actor_type = #{withdrawnByActorType},</if>
            <if test="withdrawnByActorId != null">withdrawn_by_actor_id = #{withdrawnByActorId},</if>
            <if test="clearWithdrawn != null and clearWithdrawn">
                withdrawn_at = NULL,
                withdrawn_by_actor_type = NULL,
                withdrawn_by_actor_id = NULL,
            </if>
        </set>
        WHERE id = #{id}
    </update>

    <select id="selectById" resultType="com.coursistant.lms.module.course.enrollment.entity.Enrollment">
        SELECT
        <include refid="Base_Column_List"/>
        FROM enrollment
        WHERE id = #{id}
    </select>

    <select id="selectByCourseIdAndUserId" resultType="com.coursistant.lms.module.course.enrollment.entity.Enrollment">
        SELECT
        <include refid="Base_Column_List"/>
        FROM enrollment
        WHERE course_id = #{courseId} AND user_id = #{userId}
    </select>

    <select id="selectByCourseIdAndUserIdForUpdate" resultType="com.coursistant.lms.module.course.enrollment.entity.Enrollment">
        SELECT
        <include refid="Base_Column_List"/>
        FROM enrollment
        WHERE course_id = #{courseId} AND user_id = #{userId}
        FOR UPDATE
    </select>

    <select id="selectActiveInstructorByCourseIdForUpdate" resultType="com.coursistant.lms.module.course.enrollment.entity.Enrollment">
        SELECT
        <include refid="Base_Column_List"/>
        FROM enrollment
        WHERE course_id = #{courseId}
          AND course_role = 'Instructor'
          AND active = 1
        LIMIT 1
        FOR UPDATE
    </select>

    <select id="selectActiveByUserIdForUpdate" resultType="com.coursistant.lms.module.course.enrollment.entity.Enrollment">
        SELECT
        <include refid="Base_Column_List"/>
        FROM enrollment
        WHERE user_id = #{userId} AND active = 1
        FOR UPDATE
    </select>

    <delete id="deleteById">
        DELETE FROM enrollment
        WHERE id = #{id}
    </delete>

    <select id="selectByCourseId" resultType="com.coursistant.lms.module.course.enrollment.entity.Enrollment">
        SELECT
        <include refid="Base_Column_List"/>
        FROM enrollment
        WHERE course_id = #{courseId}
        ORDER BY FIELD(course_role, 'Instructor', 'TA', 'Student'), enrolled_at
    </select>

    <select id="selectActiveByUserId" resultType="com.coursistant.lms.module.course.enrollment.entity.Enrollment">
        SELECT
        <include refid="Base_Column_List"/>
        FROM enrollment
        WHERE user_id = #{userId} AND active = 1
        ORDER BY enrolled_at DESC
    </select>

    <select id="selectActiveStudentsByCourseId" resultType="com.coursistant.lms.module.course.enrollment.entity.Enrollment">
        SELECT
        <include refid="Base_Column_List"/>
        FROM enrollment
        WHERE course_id = #{courseId}
          AND active = 1
          AND course_role = 'Student'
        ORDER BY user_id ASC
    </select>

    <select id="selectActiveByCourseId" resultType="com.coursistant.lms.module.course.enrollment.entity.Enrollment">
        SELECT
        <include refid="Base_Column_List"/>
        FROM enrollment
        WHERE course_id = #{courseId}
          AND active = 1
        ORDER BY FIELD(course_role, 'Instructor', 'TA', 'Student'), user_id ASC
    </select>

    <select id="countByCourseId" resultType="int">
        SELECT COUNT(*)
        FROM enrollment
        WHERE course_id = #{courseId}
    </select>

    <select id="countActiveInstructorsByCourseId" resultType="int">
        SELECT COUNT(*)
        FROM enrollment
        WHERE course_id = #{courseId}
          AND course_role = 'Instructor'
          AND active = 1
    </select>

    <select id="countActiveInstructorEnrollmentsByUserId" resultType="int">
        SELECT COUNT(*)
        FROM enrollment
        WHERE user_id = #{userId}
          AND course_role = 'Instructor'
          AND active = 1
    </select>

    <select id="countActiveTaEnrollmentsByUserId" resultType="int">
        SELECT COUNT(*)
        FROM enrollment
        WHERE user_id = #{userId}
          AND course_role = 'TA'
          AND active = 1
    </select>

    <select id="countActiveStudentEnrollmentsByUserId" resultType="int">
        SELECT COUNT(*)
        FROM enrollment
        WHERE user_id = #{userId}
          AND course_role = 'Student'
          AND active = 1
    </select>

    <select id="countByUserId" resultType="int">
        SELECT COUNT(*)
        FROM enrollment
        WHERE user_id = #{userId}
    </select>

</mapper>
