<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.coursistant.lms.module.assignment.repository.AssignmentAuditLogMapper">

    <sql id="Base_Column_List">
        id,
        course_id AS courseId,
        assignment_id AS assignmentId,
        actor_user_id AS actorUserId,
        action,
        detail_json AS detailJson,
        created_at AS createdAt
    </sql>

    <insert id="insert" parameterType="com.coursistant.lms.module.assignment.entity.AssignmentAuditLog"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO assignment_audit_log (
            course_id,
            assignment_id,
            actor_user_id,
            action,
            detail_json
        ) VALUES (
            #{courseId},
            #{assignmentId},
            #{actorUserId},
            #{action},
            #{detailJson}
        )
    </insert>

</mapper>
