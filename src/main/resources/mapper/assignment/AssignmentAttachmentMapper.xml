<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.coursistant.lms.module.assignment.repository.AssignmentAttachmentMapper">

    <sql id="Base_Column_List">
        id,
        assignment_id AS assignmentId,
        object_key AS objectKey,
        original_name AS originalName,
        content_type AS contentType,
        size_bytes AS sizeBytes,
        uploaded_by AS uploadedBy,
        created_at AS createdAt
    </sql>

    <insert id="insert" parameterType="com.coursistant.lms.module.assignment.entity.AssignmentAttachment"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO assignment_attachment (
            assignment_id,
            object_key,
            original_name,
            content_type,
            size_bytes,
            uploaded_by
        ) VALUES (
            #{assignmentId},
            #{objectKey},
            #{originalName},
            #{contentType},
            #{sizeBytes},
            #{uploadedBy}
        )
    </insert>

    <select id="selectById" resultType="com.coursistant.lms.module.assignment.entity.AssignmentAttachment">
        SELECT
        <include refid="Base_Column_List"/>
        FROM assignment_attachment
        WHERE id = #{id}
    </select>

    <select id="selectByAssignmentId" resultType="com.coursistant.lms.module.assignment.entity.AssignmentAttachment">
        SELECT
        <include refid="Base_Column_List"/>
        FROM assignment_attachment
        WHERE assignment_id = #{assignmentId}
        ORDER BY id
    </select>

    <delete id="deleteById">
        DELETE FROM assignment_attachment
        WHERE id = #{id}
    </delete>

</mapper>
