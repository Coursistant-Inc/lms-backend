<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.coursistant.lms.module.quiz.repository.QuizAttemptMapper">

    <sql id="Base_Column_List">
        id, quiz_id, student_id, attempt_no, started_at, submitted_at, time_spent_seconds,
        `state`, auto_score, manual_score, final_score, updated_at
    </sql>

    <select id="selectAll" resultType="com.coursistant.lms.module.quiz.entity.QuizAttempt">
        select
        <include refid="Base_Column_List"/>
        from QuizAttempt
        <where>
            <if test="id != null">and id = #{id}</if>
            <if test="quizId != null">and quiz_id = #{quizId}</if>
            <if test="studentId != null">and student_id = #{studentId}</if>
            <if test="attemptNo != null">and attempt_no = #{attemptNo}</if>
            <if test="startedAt != null">and started_at = #{startedAt}</if>
            <if test="submittedAt != null">and submitted_at = #{submittedAt}</if>
            <if test="timeSpentSeconds != null">and time_spent_seconds = #{timeSpentSeconds}</if>
            <if test="state != null">and `state` = #{state}</if>
            <if test="autoScore != null">and auto_score = #{autoScore}</if>
            <if test="manualScore != null">and manual_score = #{manualScore}</if>
            <if test="finalScore != null">and final_score = #{finalScore}</if>
            <if test="updatedAt != null">and updated_at = #{updatedAt}</if>
        </where>
    </select>

    <select id="selectById" resultType="com.coursistant.lms.module.quiz.entity.QuizAttempt">
        select
        <include refid="Base_Column_List"/>
        from QuizAttempt
        where id = #{id}
    </select>

    <delete id="deleteById">
        delete from QuizAttempt
        where id = #{id}
    </delete>

    <insert id="insert" parameterType="com.coursistant.lms.module.quiz.entity.QuizAttempt" useGeneratedKeys="true" keyProperty="id">
        insert into QuizAttempt
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">id,</if>
            <if test="quizId != null">quiz_id,</if>
            <if test="studentId != null">student_id,</if>
            <if test="attemptNo != null">attempt_no,</if>
            <if test="startedAt != null">started_at,</if>
            <if test="submittedAt != null">submitted_at,</if>
            <if test="timeSpentSeconds != null">time_spent_seconds,</if>
            <if test="state != null">`state`,</if>
            <if test="autoScore != null">auto_score,</if>
            <if test="manualScore != null">manual_score,</if>
            <if test="finalScore != null">final_score,</if>
            <if test="updatedAt != null">updated_at,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null">#{id},</if>
            <if test="quizId != null">#{quizId},</if>
            <if test="studentId != null">#{studentId},</if>
            <if test="attemptNo != null">#{attemptNo},</if>
            <if test="startedAt != null">#{startedAt},</if>
            <if test="submittedAt != null">#{submittedAt},</if>
            <if test="timeSpentSeconds != null">#{timeSpentSeconds},</if>
            <if test="state != null">#{state},</if>
            <if test="autoScore != null">#{autoScore},</if>
            <if test="manualScore != null">#{manualScore},</if>
            <if test="finalScore != null">#{finalScore},</if>
            <if test="updatedAt != null">#{updatedAt},</if>
        </trim>
    </insert>

    <update id="updateById" parameterType="com.coursistant.lms.module.quiz.entity.QuizAttempt">
        update QuizAttempt
        <set>
            <if test="quizId != null">quiz_id = #{quizId},</if>
            <if test="studentId != null">student_id = #{studentId},</if>
            <if test="attemptNo != null">attempt_no = #{attemptNo},</if>
            <if test="startedAt != null">started_at = #{startedAt},</if>
            <if test="submittedAt != null">submitted_at = #{submittedAt},</if>
            <if test="timeSpentSeconds != null">time_spent_seconds = #{timeSpentSeconds},</if>
            <if test="state != null">`state` = #{state},</if>
            <if test="autoScore != null">auto_score = #{autoScore},</if>
            <if test="manualScore != null">manual_score = #{manualScore},</if>
            <if test="finalScore != null">final_score = #{finalScore},</if>
            <if test="updatedAt != null">updated_at = #{updatedAt},</if>
        </set>
        where id = #{id}
    </update>

</mapper>
